## Compact Design（Reference Microsoft Agent Framework）

- **工具结果压缩**
  旧工具调用及结果合并为“工具名、成功/失败、关键结果、原文 ID”。当前任务仍需使用的结果保留，完整原文存入数据库或文件。工具定义按任务选择，参数 schema 不截断。

- **对话摘要**
  调用 LLM 摘要旧对话，默认保留最近 K=4 轮完整对话。一轮包含用户输入及其对应的回复和工具交互。当前题目的完整问答、纠正和必要代码额外保留，不受 K 限制。
  摘要包含：已完成事项、用户要求与纠正、已确认结论及来源消息 ID、未完成事项、待追问问题。

- **旧消息移出**
  摘要后仍超预算，优先移出已被摘要覆盖的旧消息。按完整消息组处理，工具调用与对应结果一起保留或移出。只移出模型上下文，原始历史仍可通过 Recall 查询。

- **文本截断**
  最后仅裁剪日志、重复检索片段等允许截断的大文本，按行或段落边界处理，并标注省略及原文引用。当前问题、待评分答案、代码、工具参数和规则不直接截断；必要内容过大时分块处理。

- **触发与预算**
  每次模型调用前检查完整请求的 token 数，包括 System Prompt、记忆索引、工具定义、历史和检索内容。
  输入预算 B = 模型窗口 − 输出预留 − 安全余量。初始配置：超过 B 的 80% 启动压缩，降到 B 的 60% 停止；各阶段依次执行，达到目标即停止。阈值与 K 根据评测调整。
  摘要调用也需独立控制输入和输出预算；摘要失败时保留原上下文，再尝试安全移出旧消息。受保护内容仍超过预算时，改为分块处理，不发送超限请求。

- **保留内容**
  System Prompt、Core Memory、预算内的一二级索引、当前评分标准、正在执行的 Skill 必要步骤，以及 Working Memory 中的题目进度、已覆盖能力和待办。当前请求、当前题目的原始证据及未完成工具交互保持完整。

- **与记忆衔接**
  原始历史先持久化，压缩仅改变上下文视图。对话摘要用于继续当前会话，记录来源消息范围和摘要版本；Reflection 异步提炼长期知识，更新 Core / Deferred / Skills，不将会话摘要直接当作长期事实。最终评分按来源 ID 回查关键答案。
